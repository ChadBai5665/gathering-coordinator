<view class="gathering-card" bindtap="onCardTap">
  <view class="card-header">
    <view class="card-title">{{gathering.name}}</view>
    <status-tag status="{{gathering.status}}" type="gathering" />
  </view>

  <view class="card-info">
    <view class="info-item">
      <text class="info-icon">🕐</text>
      <text class="info-text">{{formatTime(gathering.target_time)}}</text>
    </view>
    <view class="info-item">
      <text class="info-icon">👥</text>
      <text class="info-text">{{gathering.participant_count}}人</text>
    </view>
  </view>

  <view class="card-footer">
    <view class="invite-code">
      <text class="code-label">邀请码</text>
      <text class="code-value">{{gathering.code}}</text>
    </view>
  </view>
</view>
